import Section from '../Section'

function HeroSection() {
  return (
    <Section id="hero" title="Hero">
      <p className="max-w-2xl">
        Empty section ready for your headline, short intro, and call-to-action buttons.
      </p>
    </Section>
  )
}

export default HeroSection